import { FC } from 'react';
import { Button } from './styles';

const ArrowDownButton: FC = () => {
  return (
    <a href="#articles">
      <Button />
    </a>
  );
};

export default ArrowDownButton;
